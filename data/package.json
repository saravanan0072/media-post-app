{
  "name": "mock-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch data/db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0"
  }
}
